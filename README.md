# Video Analytics Telegram Bot

Бот для аналитики видео на основе естественного языка. Принимает вопросы на русском языке и возвращает числовые ответы на основе данных из PostgreSQL базы данных.

**🤖 Бот доступен:** [@abdvrash1dvideoanalytics_bot](https://t.me/abdvrash1dvideoanalytics_bot)

## 🚀 Быстрый запуск

### С Docker (рекомендуется)

1. **Клонируйте репозиторий и перейдите в директорию**
   ```bash
   git clone <your-repo-url>
   cd telegram_bot
   ```

2. **Создайте файл окружения**
   ```bash
   # Создайте .env файл с вашими токенами:
   echo "TELEGRAM_BOT_TOKEN=ваш_токен_бота" > .env
   echo "OPENAI_API_KEY=ваш_openai_ключ" >> .env
   echo "DATABASE_URL=postgresql://postgres:postgres@postgres:5432/video_analytics" >> .env
   ```

3. **Запустите с помощью Docker Compose**
   ```bash
   docker-compose up --build
   ```

### Локальный запуск

1. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

2. **Настройте базу данных PostgreSQL**
   ```bash
   # Создайте базу данных video_analytics
   createdb video_analytics
   ```

3. **Запустите миграции**
   ```bash
   python scripts/migrate.py
   ```

4. **Импортируйте данные**
   ```bash
   python scripts/import_data.py data/videos.json
   ```

5. **Создайте .env файл**
   ```bash
   # TELEGRAM_BOT_TOKEN=your_token_here
   # OPENAI_API_KEY=your_key_here
   # DATABASE_URL=postgresql://user:pass@localhost:5432/video_analytics
   ```

6. **Запустите бота**
   ```bash
   python main.py
   ```

## ⚙️ Настройка

### Переменные окружения

- `TELEGRAM_BOT_TOKEN` - токен Telegram бота (получить у [@BotFather](https://t.me/BotFather))
- `OPENAI_API_KEY` - API ключ OpenAI для обработки естественного языка
- `DATABASE_URL` - URL подключения к PostgreSQL базе данных

### Автоматическая настройка

Используйте скрипт настройки для автоматической подготовки:

```bash
python setup.py
```

## 🏗️ Архитектура

```
telegram_bot/
├── bot/
│   ├── telegram_bot.py     # Основная логика Telegram бота
│   └── nlp_processor.py    # Обработка естественного языка
├── database/
│   ├── connection.py       # Подключение к БД
│   └── schema.sql          # Схема базы данных
├── scripts/
│   ├── migrate.py          # Миграции БД
│   └── import_data.py      # Импорт данных из JSON
├── config/
│   └── settings.py         # Конфигурация приложения
├── data/
│   └── videos.json         # Исходные данные видео
├── main.py                 # Точка входа
├── requirements.txt        # Зависимости Python
├── Dockerfile             # Docker образ
├── docker-compose.yml     # Docker Compose конфигурация
└── README.md              # Документация
```

### Компоненты

1. **Telegram Bot (aiogram)** - принимает сообщения и отправляет ответы
2. **NLP Processor (OpenAI GPT-4o-mini)** - преобразует естественный язык в SQL запросы
3. **Database Layer (PostgreSQL)** - хранение и запрос данных
4. **Data Import** - загрузка JSON данных в базу

## 🔍 Как работает обработка запросов

### Схема базы данных

Бот работает с двумя таблицами:

**videos** - финальная статистика по видео:
- `id` - UUID видео
- `creator_id` - UUID создателя
- `video_created_at` - дата публикации
- `views_count`, `likes_count`, `comments_count`, `reports_count` - финальные метрики

**video_snapshots** - почасовые замеры:
- `video_id` - ссылка на видео
- `created_at` - время замера
- `views_count`, `likes_count`, etc. - значения на момент замера
- `delta_views_count`, `delta_likes_count`, etc. - приросты с прошлого замера

### Преобразование естественного языка в SQL

Используется OpenAI GPT-4o-mini для генерации SQL запросов на основе описания схемы базы данных.

#### Системный промпт

```
У тебя есть база данных с двумя таблицами:
[описание схемы]

Твоя задача: на основе вопроса пользователя на русском языке сгенерировать SQL-запрос к PostgreSQL,
который вернет ровно ОДНО число как ответ.

Формат ответа должен быть ТОЛЬКО JSON:
{
  "sql": "SELECT COUNT(*) FROM videos WHERE ...",
  "explanation": "краткое объяснение"
}
```

#### Примеры запросов

| Вопрос | SQL запрос |
|--------|------------|
| "Сколько всего видео есть в системе?" | `SELECT COUNT(*) FROM videos` |
| "Сколько видео набрало больше 100 000 просмотров?" | `SELECT COUNT(*) FROM videos WHERE views_count > 100000` |
| "На сколько просмотров выросли все видео 28 ноября 2025?" | `SELECT SUM(delta_views_count) FROM video_snapshots WHERE DATE(created_at) = '2025-11-28'` |

### Процесс обработки

1. **Получение сообщения** от пользователя
2. **Генерация SQL** через OpenAI API
3. **Выполнение запроса** в PostgreSQL
4. **Возврат числа** как ответа

## 🧪 Тестирование

### Ручное тестирование

Отправьте боту сообщения вида:
- `Сколько всего видео есть в системе?`
- `Сколько видео у креатора с id aca1061a9d324ecf8c3fa2bb32d7be63 вышло с 1 ноября 2025 по 5 ноября 2025?`
- `Сколько видео набрало больше 1000 просмотров за всё время?`
- `На сколько просмотров в сумме выросли все видео 28 ноября 2025?`
- `Сколько разных видео получали новые просмотры 27 ноября 2025?`

### Автоматическое тестирование

Для автоматической проверки отправьте служебному боту @rlt_test_checker_bot:

```
 /check @abdvrash1dvideoanalytics_bot https://github.com/yourrepo
 ```

**Примечание:** Этот бот доступен по адресу: https://t.me/abdvrash1dvideoanalytics_bot

## 📋 Требования к реализации

✅ **Выполнено:**
- Python + PostgreSQL + aiogram
- Обработка естественного языка через LLM
- Одна таблица videos (финальная статистика)
- Одна таблица video_snapshots (почасовые замеры)
- Ответы только числами
- Нет хранения контекста диалога
- Docker поддержка
- Подробная документация

## 🔧 Разработка

### Добавление новых типов запросов

1. Обновите описание схемы в `NLPProcessor.get_schema_description()`
2. Протестируйте новые типы запросов
3. Добавьте примеры в README

### Логирование

Логи пишутся в stdout. Уровень логирования настраивается в `telegram_bot.py`.

## 📄 Лицензия

MIT License

